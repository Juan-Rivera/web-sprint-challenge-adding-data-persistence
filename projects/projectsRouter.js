const express = require('express');
const router = express.Router(); // creating router with express

const taskRouter = require('../tasks/tasksRouter'); // will need tasks for later use

// endpoints (still need to do this)



module.exports = router; // exporting